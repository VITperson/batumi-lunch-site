/// <reference types="cypress" />

describe("Order flow", () => {
  it.skip("allows customer to create, update and cancel order", () => {
    // TODO: Implement end-to-end flow once backend test harness is available.
    // Expected steps:
    // 1. Login via API and set auth cookies.
    // 2. Navigate to /order/new and submit order wizard.
    // 3. Visit /orders/{id}, update count, then cancel the order.
    // 4. Assert status transitions via API.
  });
});
