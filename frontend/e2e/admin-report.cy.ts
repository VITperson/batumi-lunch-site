/// <reference types="cypress" />

describe("Admin reports", () => {
  it.skip("displays weekly aggregation", () => {
    // TODO: Implement once fixture data and admin auth automation are ready.
    // Steps: login as admin, open /admin/reports, verify table totals for seeded data.
  });
});
